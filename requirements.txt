# Install setuptools first to fix distutils issue in Python 3.12
setuptools>=68.0.0
wheel>=0.41.0

# Data processing
# Note: numpy>=2.0.0 required for compatibility with thinc (if installed)
# If you encounter issues with numpy 2.0, you can pin to 1.26.x
pandas>=2.1.0
numpy>=2.0.0,<2.1.0
scikit-learn>=1.3.0
statsmodels>=0.14.0

# Forecasting models
# Note: Prophet may have issues with Python 3.12. If installation fails, 
# try: pip install prophet --no-build-isolation
prophet>=1.1.5
xgboost>=2.0.0
tensorflow>=2.15.0
# Optional: pmdarima for better ARIMA auto-tuning (install separately if needed)
# pip install pmdarima

# API framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6

# Utilities
openpyxl>=3.1.0
joblib>=1.3.0
matplotlib>=3.8.0
seaborn>=0.13.0
holidays>=0.36
requests>=2.31.0
